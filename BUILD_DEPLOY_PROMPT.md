I want you to perform a FULL CLEAN BUILD & DEPLOY pipeline.
Follow the steps EXACTLY in this order.
Do NOT skip any step.
Do NOT assume anything already exists.
Target: Web + Firebase Hosting + Git + Signed Android APK.

=====================================
STEP 1: FULL CLEAN (SAFE)
=====================================

1. Remove all old web build artifacts:
   - dist/
   - build/ (if exists)

2. Remove Android generated assets:
   - android_app/app/src/main/assets/
   - android_app/app/build/

3. Clear Capacitor cache (if any):
   - android_app/.gradle/
   - android_app/app/.cxx/ (if exists)

4. DO NOT delete:
   - node_modules
   - android keystore (.jks)
   - firebase config files

=====================================
STEP 2: FRESH WEB BUILD
=====================================

1. Install dependencies (safe check):
   npm install

2. Create fresh production build:
   npm run build

3. Verify:
   - dist/index.html exists
   - dist/assets/* exists
   - No build errors

=====================================
STEP 3: FIREBASE DEPLOY (WEB)
=====================================

1. Ensure firebase.json points to:
   "public": "dist"

2. Deploy to Firebase Hosting:
   firebase deploy --only hosting

3. Verify:
   - Live URL loads correctly
   - No 404
   - Latest UI changes visible

=====================================
STEP 4: GIT DEPLOY
=====================================

1. Check status:
   git status

2. Add changes:
   git add .

3. Commit with clear message:
#Generate a meaningful commit message automatically
   based on the actual changes (do NOT use a fixed message).
   git commit -m "<auto-generated message>"

4. Push to remote:
   git push origin main

=====================================
STEP 5: ANDROID (CAPACITOR) SYNC
=====================================

1. Ensure Android platform exists:
   npx cap add android   (ONLY if not already added)

2. Sync fresh web build to Android:
   npx cap sync android

3. Verify:
   - android_app/app/src/main/assets/index.html exists
   - assets updated from dist/

=====================================
STEP 6: SIGNED APK BUILD
=====================================

1. Navigate to android folder:
   cd android_app

2. Build signed release APK:
   ./gradlew assembleRelease



3. Verify:
   - APK installs successfully
   - App opens without crash
   - Offline mode works
   - Tags / filters / counts work

=====================================
FINAL CONFIRMATION REQUIRED
=====================================

After completion, report:
- Firebase deploy SUCCESS
- Git push SUCCESS
- Capacitor sync SUCCESS	
- APK signed build SUCCESS


DO NOT optimize or refactor code.
ONLY clean, build, and deploy safely.
